# Daily Checker — 컴시간 ↔ NEIS 시간표 대조

매일 아침 전 학급을 눈으로 훑던 작업을 대신합니다.

**컴시간이 항상 기준입니다.** NEIS를 컴시간에 맞춰야 할 자리만 뽑아냅니다.

## 폴더에 든 것

| 파일 | 쓰임 |
|---|---|
| `Daily Checker.html` | 이것 하나가 프로그램 전부입니다 |

설치할 것이 없습니다. 인터넷 연결도 필요 없습니다.
엑셀 파일을 푸는 일까지 브라우저가 직접 합니다.

---

## 준비 (처음 한 번만)

없습니다. 엣지(Edge)나 크롬만 있으면 됩니다. 윈도우 10·11에는 엣지가 들어 있습니다.

`Daily Checker.html` 을 더블클릭했을 때 다른 프로그램이 열리면,
파일을 오른쪽 클릭 → **연결 프로그램 → Microsoft Edge** 를 고르시면 됩니다.
`항상 이 앱으로 열기` 를 켜 두시면 다음부터는 그냥 더블클릭하면 됩니다.

**바탕화면에 두고 쓰시려면** 파일을 오른쪽 클릭 → `바로 가기 만들기` 한 뒤
그 바로 가기를 바탕화면으로 옮기세요.

---

## 매일 하는 일

1. 컴시간에서 전체시간표를 엑셀로 내려받습니다 (**내부망 접속, 가상학급 출력 켜기**)
2. NEIS에서 학년별 일일 시간표를 엑셀로 내려받습니다
3. `Daily Checker.html` 을 더블클릭합니다
4. 컴시간 파일 1개, NEIS 파일을 고릅니다 — NEIS는 **한 번에 여러 개 선택**할 수 있습니다
   - 파일을 창 안으로 **끌어다 놓아도** 됩니다. 컴시간 줄에는 컴시간 파일, NEIS 줄에는 NEIS 파일을 놓으세요.
5. `대조 시작` 을 누릅니다

**날짜는 프로그램이 먼저 확인합니다.** 서로 다른 날 파일을 고르시면 대조하지 않고 그 사실만
알려 줍니다. 잘못 받은 파일로 목록이 수십 건 쏟아지는 일은 없습니다.

**내부망으로 받으셔야 합니다.** 외부망에서 받으면 교사명이 앞 두 자만 나와서 대조가 부정확해집니다.

**파일은 이 PC 밖으로 나가지 않습니다.** 브라우저 안에서만 읽고 끝냅니다.
인터넷이 끊긴 내부망 PC에서도 그대로 동작합니다.

## 결과 읽는 법

고칠 자리가 없으면 **일치** 라고만 뜹니다. 그게 정상 상태입니다.

있으면 자리마다 카드가 하나씩 나옵니다.

```
☐  3학년 8반  3교시     교사 다름
   NEIS      독서 (김교사)
   컴시간   고전B (이교사)   ← 이대로 고치기
```

NEIS에서 해당 학년·반·교시를 찾아 컴시간 쪽으로 고치시면 됩니다.

**고친 자리는 왼쪽 칸을 눌러 표시하세요.** 글자가 흐려지고 위쪽 숫자가 올라갑니다.
여러 건일 때 어디까지 했는지 놓치지 않으려고 넣은 기능입니다.

**목록 복사** 를 누르면 전체 목록이 글자로 복사됩니다. 메신저로 보내거나 메모에 붙여 두실 때 쓰세요.

## 열리지 않을 때

- 흰 화면만 나오고 아무것도 없으면 → 엣지나 크롬으로 열렸는지 확인하세요. 인터넷 익스플로러에서는 동작하지 않습니다.
- `엑셀 파일(.xlsx)이 아닙니다` 가 뜨면 → 확장자가 `.xls` 인 옛 형식입니다. 엑셀에서 열어 `.xlsx` 로 저장한 뒤 쓰세요.
- 파일을 고르는 창이 안 뜨면 → 버튼 대신 파일을 창 안으로 끌어다 놓아 보세요.

---

## 대조 규칙

### 날짜부터 맞춰 봅니다

**같은 날 시간표일 때만 대조합니다.** 다른 날 파일을 맞대면 거의 모든 칸이 다르다고 나와서
목록이 아무 쓸모가 없기 때문입니다. 그래서 대조에 앞서 날짜부터 봅니다.

- 컴시간은 왼쪽 위에 `목 (17)` 처럼 요일과 날짜가 적혀 있습니다
- NEIS는 맨 윗줄에 `2026.09.17.(목요일)` 이 적혀 있습니다

요일과 날짜가 어긋나면 **목록 대신 이렇게 뜹니다.**

```
컴시간과 NEIS가 서로 다른 날입니다.

  컴시간 전체시간표.xlsx              월요일 14일
  NEIS 시간표 2학년 목요일.xlsx      2026.09.17.(목요일)

같은 날 시간표를 고르셨는지 확인해 주세요. 대조는 하지 않았습니다.
```

NEIS 파일을 여러 개 고르셨는데 **그중 하나만 다른 날**이어도 같은 방식으로 멈춥니다.
어느 파일이 어긋났는지 파일 이름과 날짜가 같이 나오니 그것만 다시 받으시면 됩니다.

컴시간에는 연도와 달이 없어서 **요일과 일자**로 맞춥니다. 한 달을 통째로 잘못 받으시면서
요일과 일자가 우연히 같은 경우는 걸러내지 못합니다. 그런 날은 드뭅니다.

### 칸을 맞대는 방식

| 상황 | 비교 방식 |
|---|---|
| 일반 교시 | 과목 + 교사 둘 다 |
| 이동수업 | 교사만, 그 교시 전체에서 찾음 |
| 10반 (가상학급) | 교사만, 그 교시 전체에서 찾음 |
| 공동교육과정 `(공동)` | 비교하지 않음 |
| NEIS에만 있는 강좌 | 비교하지 않음 |

10반은 가상학급이라 NEIS에 반 이름 없이 교실명만 나옵니다. 대응할 행이 없으므로 이동수업과 같은 방식으로 처리합니다.

이동수업에서 교사만 보는 이유는, 그 시간에는 결보강이나 교사 교체만 일어나고 다른 시간과의 교체는 없기 때문입니다.

그리고 이동수업은 학생이 원적반을 떠나므로, 컴시간의 반 칸과 NEIS의 같은 반 칸을 맞대면 어긋납니다. 대신 **그 교시 NEIS 전체에서 해당 교사를 찾습니다.** 도서관·미래교실 같은 특별실 행도 포함해서요. 교사가 어딘가에 있으면 정상으로 봅니다.

### 공동교육과정

공동교육과정은 NEIS에 8·9교시로, 그것도 원적반 행이 아니라 `생명실` 같은 특별실 행에만
`(공동)생명과학 실험` 처럼 적힙니다. 컴시간은 **7교시까지만 내보내므로** 이 자리는 애초에
대조 대상이 되지 않습니다.

컴시간에서는 공동교육과정이 **가상학급(10반) 칸**에 `공유` 로 들어갑니다. 가상학급이므로
교사만 그 교시 전체에서 찾습니다. 정상으로 넘어갑니다.

혹시 컴시간 설정이 바뀌어 8·9교시가 나오더라도, `(공동)` 표시가 붙은 칸은 대조하지 않습니다.
표시가 **없는** 8·9교시 수업은 그대로 대조합니다.

### NEIS의 `(보)` 표시는 무시합니다

NEIS에 `(보)스포츠 생활`, 컴시간에 `체육3` 으로 적힌 자리는 **보강 표시일 뿐 같은 과목**입니다.
교사도 같다면 고칠 자리가 아니므로 목록에 올리지 않습니다.
과목명 앞에 붙은 `(보)` `(대)` 같은 세 글자 이내의 괄호 표시를 떼고 맞대어 봅니다.

떼는 것은 **비교할 때뿐**입니다. 다른 이유로 목록에 오른 자리라면 NEIS에 적힌 그대로
`(보)스포츠 생활` 로 보여 드립니다. NEIS에서 그 칸을 찾으실 때 헷갈리지 않도록요.

---

## 과목명 대응표

컴시간은 `고전A`, NEIS는 `고전 읽기`처럼 과목명 표기가 다릅니다. 이 대응표는 **프로그램이 알아서 만듭니다.**

교사가 일치하는 칸을 찾아 "이 교사가 컴시간에서는 `논리`, NEIS에서는 `논리학`을 가르치니 둘은 같은 과목"이라고 추론하는 방식입니다. 실행할 때마다 새 과목이 쌓입니다.

처음 며칠은 대응표가 비어 있어 과목 불일치를 못 잡을 수 있습니다. **교사 불일치는 처음부터 잡히니** 실질적인 문제는 아니지만, 며칠 돌려서 대응표가 채워지면 더 촘촘해집니다.

### 대응표는 브라우저가 들고 있습니다

브라우저는 PC의 파일을 마음대로 쓸 수 없습니다.
그래서 대응표는 **이 PC의 브라우저 안에** 저장됩니다. 창을 닫아도, PC를 꺼도 남아 있습니다.

창 아래쪽 오른편에 대응표 개수와 버튼 세 개가 있습니다.

- **내보내기** — 대응표를 `subject_map.json` 파일로 내려받습니다
- **가져오기** — 내려받아 둔 파일을 다시 불러옵니다
- **지우기** — 대응표를 비웁니다

**한 달에 한 번쯤 `내보내기` 를 눌러 파일로 남겨 두세요.**
브라우저 사용 기록(쿠키·사이트 데이터)을 지우거나 PC를 바꾸면 대응표가 사라집니다.
그때 `가져오기` 로 되살리시면 됩니다.

**학기가 바뀌면 `지우기` 를 누르고 새로 학습시키세요.** 과목 편성이 달라지면 기존 대응표가 어긋납니다.

대응표는 이런 형태입니다. 키 앞의 숫자는 학년입니다.

```json
{
  "1-체육1": "체육2",
  "2-체육2": "스포츠 문화",
  "3-체육3": "스포츠 생활",
  "3-고전A": "고전 읽기",
  "3-고전B": "고전 읽기"
}
```

**학년을 붙이는 이유**가 있습니다. 같은 체육이라도 학년마다 공식 과목명이 달라서(체육2 / 스포츠 문화 / 스포츠 생활), 학년을 빼면 서로 덮어써 버립니다.

**과목명 뒤의 숫자나 알파벳도 떼지 않습니다.** `고전A`의 A는 분반 기호지만 `공통국어2`의 2는 과목명의 일부라, 겉모습만으로는 구분할 수 없기 때문입니다.

---

## 알아두실 점

**이 프로그램은 NEIS를 건드리지 않습니다.** 읽기만 하고, 고칠 자리를 알려줄 뿐입니다.
수정은 선생님이 직접 하십니다. 프로그램이 틀려도 손해가 "목록이 이상하다" 수준에서 끝나도록 만들었습니다.

**확인된 것**

- **1·2·3학년 전체 + 가상학급 대조**, NEIS 파일 3개를 한 번에 넣는 경우까지
- 교사 다름 · 과목 다름 · NEIS 비어 있음 · 가상학급 교사 없음 · 이동수업 교사 없음 다섯 가지 판정 모두
- 이동수업·가상학급 칸을 그 교시 전체에서 찾아 정상으로 넘기는 경로
- 대응표가 비어 있을 때와 채워져 있을 때 모두
- 화면·체크 표시·대응표 저장까지 실제로 동작

**날짜 확인**도 세 가지로 시험했습니다.

- 컴시간(월) + NEIS(목) → 대조하지 않고 멈춤 ✓
- NEIS 두 개가 서로 다른 날 → 대조하지 않고 멈춤 ✓
- 같은 날 → 정상 대조 ✓

**아직 확인되지 않은 것**

- 시험 기간처럼 시간표가 통째로 다른 날
- 학기가 바뀌어 과목 편성이 달라지는 시점

**당분간은 눈으로 하던 점검을 함께 하세요.** 결과와 실제가 맞는지 1~2주 확인한 뒤에 신뢰하시는 게 안전합니다.

**반 수는 헤더에서 자동으로 읽습니다.** 가상학급을 켜든 끄든, 학년마다 반 수가 다르든 알아서 맞춥니다. 다만 컴시간을 매번 같은 설정으로 받으시는 편이 결과를 비교하기 좋습니다.

**컴시간이나 NEIS의 파일 형식이 바뀌면** 프로그램이 멈추거나 이상한 결과를 냅니다. 결과가 갑자기 이상해지면 그 가능성을 먼저 의심해 보세요.

---

만든이 **yeoniis** · 문의 [shinhsif2024@gmail.com](mailto:shinhsif2024@gmail.com?subject=Daily%20Checker%20%EB%AC%B8%EC%9D%98)

© 2026 yeoniis
